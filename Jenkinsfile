pipeline {
    agent { label "linux" }
    stages {
        stage('Checkout') {
            steps { }
        }
        stage('Build') {
            steps { }
        }
        stage('Test') {
            steps { }
        }
        stage('Docker Build') {
            steps { }
        }
    }
}
